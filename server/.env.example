# Mongo
MONGODB_URI=mongodb://127.0.0.1:27017/zuari_travel_desk

# Also supported (alias):
MONGO_URI=

# Auth
JWT_SECRET=change_me_to_a_long_random_string

# OTP + Email
# EMAIL_MODE=console prints OTPs to server console (good for dev)
# EMAIL_MODE=smtp uses SMTP_* settings below
EMAIL_MODE=console
OTP_TTL_MINUTES=10
OTP_RESEND_COOLDOWN_SECONDS=30

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Zuari Travel Desk <no-reply@zuari.local>"

# Optional aliases for Outlook/Office365 accounts:
OUTLOOK_USER=
OUTLOOK_PASS=

# Server
PORT=8787
CORS_ORIGIN=http://localhost:3000
